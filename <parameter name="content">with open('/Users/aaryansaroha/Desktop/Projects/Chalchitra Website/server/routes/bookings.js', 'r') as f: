content = f.read()

# Fix the SQL query to include u.name from users table
old_query = "SELECT b.*, b.qr_code, b.selected_seats, b.ticket_html, m.title, m.date, m.venue, m.poster_url, m.price FROM bookings b JOIN movies m ON b.movie_id = m.id WHERE b.user_id = ? ORDER BY b.created_at DESC"
new_query = "SELECT b.*, b.qr_code, b.selected_seats, b.ticket_html, m.title, m.date, m.venue, m.poster_url, m.price, u.name FROM bookings b LEFT JOIN movies m ON b.movie_id = m.id LEFT JOIN users u ON b.user_id = u.id WHERE b.user_id = ? ORDER BY b.created_at DESC"

content = content.replace(old_query, new_query)

with open('/Users/aaryansaroha/Desktop/Projects/Chalchitra Website/server/routes/bookings.js', 'w') as f:
    f.write(content)

print('Updated SQL query to include user name!')
</parameter>
<parameter name="path">/Users/aaryansaroha/Desktop/Projects/Chalchitra Website/fix_query.py</parameter>
